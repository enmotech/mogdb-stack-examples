{{- if .Values.withCRD }}
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.5.0
  creationTimestamp: null
  name: multitables.mogdb.enmotech.io
spec:
  group: mogdb.enmotech.io
  names:
    kind: MultiTable
    listKind: MultiTableList
    plural: multitables
    singular: multitable
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: status
      type: string
    - jsonPath: .status.current.name
      name: db
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        description: MultiTable defines multi cluster multi table
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            properties:
              cluster:
                description: Cluster points to the multi cluster where the table in.
                properties:
                  apiVersion:
                    description: API version of the referent.
                    type: string
                  fieldPath:
                    description: 'If referring to a piece of an object instead of an entire object, this string should contain a valid JSON/Go field access statement, such as desiredState.manifest.containers[2]. For example, if the object reference is to a container within a pod, this would take on a value like: "spec.containers{name}" (where "name" refers to the name of the container that triggered the event) or if no container name is specified "spec.containers[2]" (container with index 2 in this pod). This syntax is chosen only to have some well-defined way of referencing a part of an object. TODO: this design is not final and this field is subject to change in the future.'
                    type: string
                  kind:
                    description: 'Kind of the referent. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
                    type: string
                  name:
                    description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names'
                    type: string
                  namespace:
                    description: 'Namespace of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/'
                    type: string
                  resourceVersion:
                    description: 'Specific resourceVersion to which this reference is made, if any. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency'
                    type: string
                  uid:
                    description: 'UID of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#uids'
                    type: string
                type: object
              dataCenters:
                description: DataCenters is a list of the DCs in the cluster.
                items:
                  properties:
                    kubeContextName:
                      description: KubeContextName points to kubeClientConfig which is used to connect to the data center.
                      type: string
                    name:
                      type: string
                    namespace:
                      type: string
                    region:
                      type: string
                  type: object
                type: array
              template:
                description: Table is table spec.
                properties:
                  alterDefinition:
                    description: AlterDefinition use alterOption if you need to alter the table. Instead of modifying the definition. More info see https://docs.mogdb.io/zh/mogdb/v5.0/ALTER-TABLE
                    type: string
                  database:
                    description: Database is the database name where table in.
                    maxLength: 63
                    minLength: 1
                    pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                    type: string
                  definition:
                    description: 'Definition is the definition of the table. More info: https://docs.mogdb.io/zh/mogdb/v5.0/CREATE-TABLE'
                    type: string
                  name:
                    description: 'Name is the name of the table. More create table info: https://docs.mogdb.io/zh/mogdb/v5.0/CREATE-TABLE'
                    maxLength: 63
                    minLength: 1
                    pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                    type: string
                  schema:
                    default: public
                    description: 'Schema is the schema of the table. More create table info: https://docs.mogdb.io/zh/mogdb/v5.0/CREATE-TABLE'
                    maxLength: 63
                    minLength: 1
                    pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                    type: string
                  source:
                    description: Source points to the table you want to rename.
                    pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                    type: string
                  tableType:
                    description: 'TableType is the type of table. Support: UNLOGGED, LOCAL TEMP, GLOBAL TEMP.'
                    enum:
                    - UNLOGGED
                    - LOCAL TEMP
                    - GLOBAL TEMP
                    type: string
                type: object
            required:
            - cluster
            type: object
          status:
            properties:
              current:
                type: string
              dataCenters:
                description: DataCenters is a list of the DC's status in the cluster.
                items:
                  properties:
                    kubeContextName:
                      description: KubeContextName points to kubeClientConfig which is used to connect to the data center.
                      type: string
                    name:
                      type: string
                    namespace:
                      type: string
                    region:
                      type: string
                    status:
                      properties:
                        current:
                          type: string
                        phase:
                          description: The phase of a MogdbTable is a simple, high-level summary of where the MogdbTable is in its lifecycle.
                          type: string
                        revision:
                          description: Revision Identifies the databases that have been installed.
                          type: string
                      type: object
                  type: object
                type: array
              phase:
                description: The phase of a MultiTable is a simple, high-level summary of where the MultiTable is in its lifecycle.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
{{- end }}