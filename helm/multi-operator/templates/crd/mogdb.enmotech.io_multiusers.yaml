{{- if .Values.withCRD }}
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.5.0
  creationTimestamp: null
  name: multiusers.mogdb.enmotech.io
spec:
  group: mogdb.enmotech.io
  names:
    kind: MultiUser
    listKind: MultiUserList
    plural: multiusers
    singular: multiuser
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: status
      type: string
    - jsonPath: .status.current.name
      name: db
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        description: MultiUser defines multi cluster multi user
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            properties:
              cluster:
                description: Cluster points to the mogdb cluster where the user in.
                properties:
                  apiVersion:
                    description: API version of the referent.
                    type: string
                  fieldPath:
                    description: 'If referring to a piece of an object instead of an entire object, this string should contain a valid JSON/Go field access statement, such as desiredState.manifest.containers[2]. For example, if the object reference is to a container within a pod, this would take on a value like: "spec.containers{name}" (where "name" refers to the name of the container that triggered the event) or if no container name is specified "spec.containers[2]" (container with index 2 in this pod). This syntax is chosen only to have some well-defined way of referencing a part of an object. TODO: this design is not final and this field is subject to change in the future.'
                    type: string
                  kind:
                    description: 'Kind of the referent. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
                    type: string
                  name:
                    description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names'
                    type: string
                  namespace:
                    description: 'Namespace of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/'
                    type: string
                  resourceVersion:
                    description: 'Specific resourceVersion to which this reference is made, if any. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency'
                    type: string
                  uid:
                    description: 'UID of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#uids'
                    type: string
                type: object
              dataCenters:
                description: DataCenters is a list of the DCs in the cluster.
                items:
                  properties:
                    kubeContextName:
                      description: KubeContextName points to kubeClientConfig which is used to connect to the data center.
                      type: string
                    name:
                      type: string
                    namespace:
                      type: string
                    region:
                      type: string
                  type: object
                type: array
              user:
                description: User is user spec.
                properties:
                  alterOption:
                    description: AlterOption use alterOption if you need to alter the database. Instead of modifying the Option. More info see https://docs.mogdb.io/zh/mogdb/v5.0/ALTER-USER
                    type: string
                  grantSQL:
                    description: GrantSQL use GrantSQL if you need to grant to user. More info see https://docs.mogdb.io/zh/mogdb/v5.0/GRANT
                    type: string
                  name:
                    description: Name is the name of the user.
                    maxLength: 63
                    minLength: 1
                    pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                    type: string
                  option:
                    description: 'Option are additional option of the database. More info: https://docs.mogdb.io/zh/mogdb/v5.0/CREATE-USER'
                    type: string
                  password:
                    description: Password is the password of the user.
                    properties:
                      password:
                        description: Password is the password of the user.
                        pattern: ^[A-Za-z0-9~!@#$%^&*()\-=+|\[\]{};:,<.>\/?]{8,}$
                        type: string
                      passwordDisable:
                        description: Disable whether the password is disabled.
                        type: boolean
                      passwordExpired:
                        description: Expired whether the password is expired.
                        type: boolean
                    type: object
                  revokeSQL:
                    description: RevokeSQL use RevokeSQL if you need to revoke to user. More info see https://docs.mogdb.io/zh/mogdb/v5.0/REVOKE
                    type: string
                  source:
                    description: Source points to the user you want to rename.
                    type: string
                type: object
            type: object
          status:
            properties:
              current:
                type: string
              dataCenters:
                description: DataCenters is a list of the DC's status in the cluster.
                items:
                  properties:
                    kubeContextName:
                      description: KubeContextName points to kubeClientConfig which is used to connect to the data center.
                      type: string
                    name:
                      type: string
                    namespace:
                      type: string
                    region:
                      type: string
                    status:
                      properties:
                        current:
                          type: string
                        grantRevision:
                          description: GrantRevision Identifies the users that have been installed.
                          type: string
                        history:
                          description: History Records all executed SQL statements.
                          items:
                            type: string
                          type: array
                        phase:
                          description: The phase of a MogdbUser is a simple, high-level summary of where the MogdbUser is in its lifecycle.
                          type: string
                        revision:
                          description: Revision Identifies the users that have been installed.
                          type: string
                        revokeRevision:
                          description: RevokeRevision Identifies the users that have been installed.
                          type: string
                      type: object
                  type: object
                type: array
              phase:
                description: The phase of a MultiUser is a simple, high-level summary of where the MultiUser is in its lifecycle.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
{{- end }}